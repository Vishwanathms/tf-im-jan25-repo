pipeline {
  agent any
  triggers {
    pollSCM('H/3 * * * *')
  }
  stages {
    stage('Test stage') {
      steps {
        sh 'ls -l'
        sh 'pwd'
        sh 'terraform -version'
        sh 'aws sts get-caller-identity'
        sh 'which ping'
      }
    }
  }
}