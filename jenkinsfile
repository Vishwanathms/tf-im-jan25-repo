pipeline {
  agent any
  stages {
    stage('Test stage') {
      steps {
        sh 'ls -l'
        sh 'pwd'
        sh 'terraform -version'
        sh 'aws sts get-caller-identity'
      }
    }
  }
}